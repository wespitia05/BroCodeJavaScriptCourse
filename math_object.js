// Math = built in object that provides a
//        collection of properties and methods

console.log(Math.PI);

let x = 3.21;
let y = 2.99;
let z = 1;

// z = Math.round(x); // rounds regularly
// z = Math.floor(y); // rounds down
// z = Math.ceil(x); // rounds up
// z = Math.trunc(x); // removes anything after the decimal
// z = Math.pow(x, y); // 3.21^2.99
// z = Math.sqrt(x); // squareroot
// z = Math.log(x); // logarithm
// z = Math.sin(x); // sine
// z = Math.cos(x); // cosine
// z = Math.tan(x); // tangent
// z = Math.abs(x); // absolute value
// z = Math.sign(x);
let max = Math.max(x, y, z); 
let min = Math.min(x, y ,z);

console.log(z);
console.log(max);
console.log(min);